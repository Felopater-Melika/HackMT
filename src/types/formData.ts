type FormDataType = {
  cptCode: string;
  hospitalPrice: number;
  normalPrice: number;
  description: string;
  highlight: boolean;
};

type FormDataTypeArray = FormDataType[];

export default FormDataTypeArray;
